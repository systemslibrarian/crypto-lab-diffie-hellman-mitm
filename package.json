{
  "name": "crypto-lab-diffie-hellman-mitm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types --no-warnings --test scripts/engine.test.mjs",
    "smoke": "node scripts/smoke.mjs",
    "axe": "node scripts/axe.mjs",
    "test:a11y": "playwright test",
    "verify": "npm run build && npm test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "typescript": "~5.6.3",
    "vite": "^8.2.0"
  }
}
